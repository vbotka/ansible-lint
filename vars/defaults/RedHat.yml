---
# RedHat vars for ansible_lint

mal_packages: "{{ mal_packages_redhat }}"
mal_links: "{{ mal_links_redhat }}"
mal_pip_executable: "{{ pip_executable_redhat }}"
mal_pip_requirements: "{{ pip_requirements_redhat }}"

# EOF
...
