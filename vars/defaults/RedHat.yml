---
# RedHat vars for ansible_lint

mal_pip_install: false

mal_packages:
  - name: ansible-lint

mal_pip_executable: "{{ pip_executable_redhat }}"

# moved to defaults/main.yml
# mal_links: []

# EOF
...
