---
# FreeBSD vars defaults for ansible_lint

mal_packages: "{{ mal_packages_freebsd }}"
mal_links: "{{ mal_links_freebsd }}"
mal_pip_executable: "{{ pip_executable_freebsd }}"
mal_pip_requirements: "{{ pip_requirements_freebsd }}"

# EOF
...
